import {get} from "../Services/ServiceAPI"
const modulesUrl = {
    "product" : "products.json"
}
export default class {
    protected async list() : Promise<Array<any>> {
        // let result = get("")
        return []
    }
}